package problem2;

/**
 * The Cuboid interface represents a "cuboid" with specific width, height and depth.
 */
public interface Cuboid {
  boolean isValid(Integer width, Integer height, Integer depth);
}